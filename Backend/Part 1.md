Which Package provides access to DOM and server renders for React?
React-DOM

What method is used to create a root for rendering a React application?
ReactDOM.createRoot()

What is the primary purpose of `React.createElement`?

To create and stamp out a reusable component instance, similar to creating a stamp that can be used multiple times