
we must finish the previous command before going to the next xcommand
which is a problem, as it is synchronoudly executed,

Synchornous js cannot do multiply stuff at the same time

why does the me frist gets printed at first,
it is cause js is not complete, or enough on its own, we always need promisses, call stack web api event loop as the components to get sall the ability of JS

Even with a zero-millisecond delay, the `setTimeout()` callback will not immediately execute. Instead, 'me first' will be logged before the callback function, demonstrating JavaScript's non-blocking, asynchronous behavior.

### JS engine and web API

THESE ARE THE EXAMPLES OF THE FACADE FUNCTION IN JS 
it never runs on isloation it noramally runs on a js
The web gives JS web dev, web sockets, The browser makes the network request for the JS engine, it makes network requests, it provides rendering (html DOM) 

JS does not even have the feature of a Timer, which is also included in the web browser, settimeout is also not part of the raw JS... the document of (document.querySelector) is also present in the web browser



## Web browsr Features

